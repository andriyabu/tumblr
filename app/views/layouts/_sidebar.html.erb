<section class="col-md-2 card bg-info m-3 p-0">
  <article class="card-header">
    <h2 class="text-center">Menu</h2>
  </article>
  <article class="card-body">
  
  <% if user_signed_in? %>
    <p> Users : <%= current_user.email %> </p>
    
    <%= link_to "Sign out", destroy_user_session_path, method: :delete, class:"btn btn-danger btn-sm btn-block"%>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class:"btn btn-sm btn-success btn-block"%>
  <% end %>
  
    
    
  </article>
</section>
