<h1>
  All Post
</h1>

<% @posts.each do |post| %>
<div class="card">
  <div class="card-header">
    <h2>
      <%= post.title %>
    </h2>
  </div>
  <div class="card-body">
    <%= time_ago_in_words(post.created_at) %>
    <br>
    <%= post.body %>
  </div>
</div>
<% end %>
